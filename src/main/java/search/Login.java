package search;

import org.openqa.selenium.WebDriver;
import pageTemplate.Page;

public class Login extends Page {
    public Login(WebDriver driver) {
        super(driver);
    }

    public void login(String username, String password) {
//        driver.
    }
}
